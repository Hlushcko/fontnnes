package com.example.test349;

public class DatabaseLogic {

    //private final DatabaseReference fontnnesDatabaseUser = FirebaseDatabase.getInstance().getReference("User");
    //private final DatabaseReference fontnnesDatabaseUserInfo = FirebaseDatabase.getInstance().getReference("User_info");


    public void PushInfoUser(String email, String lname, String fname, String password){

       // String id = fontnnesDatabaseUser.getKey();
      // fontnnesDatabaseUser.push().setValue(new User(id, email, lname, fname, password));

    }

    public void PushDetailedInfoUser(String date, String search, String gender, String alcohol, String smocking, String info){

    //    String id = fontnnesDatabaseUserInfo.getKey();
      //  fontnnesDatabaseUserInfo.push().setValue(new User(id, date, search, gender, alcohol, smocking, info));

    }

}

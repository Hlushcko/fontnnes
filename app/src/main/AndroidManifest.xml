<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fontnnes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Test349">
        <activity android:name="com.example.fontnnes.Account"></activity>
        <activity android:name="com.example.fontnnes.Search" />
        <activity android:name="com.example.fontnnes.Message" />
        <activity android:name="com.example.fontnnes.Home" />
        <activity android:name="com.example.fontnnes.Weryfication_register" />
        <activity android:name="com.example.fontnnes.Register_activity" />
        <activity
            android:name="com.example.fontnnes.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>